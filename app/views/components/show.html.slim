p#notice = notice

= show_for @component do |f|
  = f.attributes :name

= link_to t('helpers.links.edit'), edit_company_component_path(@company, @component)
'|
= link_to t('helpers.links.back'), company_components_path
