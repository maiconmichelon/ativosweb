= simple_form_for [current_company, @component], html: { class: 'form-horizontal' }, wrapper: :horizontal_form  do |f|
  = f.error_notification

  = f.input :name

  = f.submit class: 'btn btn-success'
