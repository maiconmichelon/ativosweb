<%= simple_form_for @type, :html => { :class => 'form form-horizontal' } do |f| %>
  <%= f.input :description %>
  <%= f.input :initial_code %>
  <%= f.button :submit %>
  <%= back_button types_path %>
<% end %>
