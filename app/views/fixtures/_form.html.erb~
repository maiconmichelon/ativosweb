<%= form_for @fixture, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :number, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :number, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :warranty, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :warranty, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :provider_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.collection_select :provider_id, Provider.activated, :id, :name %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :acquisition, :class => 'control-label' %>
    <div class="controls">
      <%= f.datetime_local_field :acquisition %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :type_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.collection_select :type_id, Type.activated, :id, :description %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :person_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.collection_select :person_id, Person.activated, :id, :name%>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :purchaseValue, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :purchaseValue, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                fixtures_path, :class => 'btn' %>
  </div>
<% end %>
