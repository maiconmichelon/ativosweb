<%= simple_form_for @fixture, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :number %>
  <%= f.input :warranty %>
  <%= f.association :provider %>
  <%= f.input :acquisition, as: :bootstrap_datepicker %>
  <%= f.association :type %>
  <%= f.association :person %>
  <%= f.input :purchaseValue %>

  <%= render 'layouts/nested_form',
      f: f,
      association: :component_fixtures,
      attributes: [:description, :quantity, :component] %>

  <%= f.submit class: 'btn btn-primary' %>
  <%= back_button fixtures_path %>
<% end %>
