<%= simple_form_for @person, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= f.association :group %>
  <%= f.submit class: 'btn btn-primary' %>
  <%= back_button people_path %>
<% end %>
